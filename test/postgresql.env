HOST=
PORT=5432
USER=
PASSWORD=
DATABASE=
SCHEMA=
TABLE=
TIMESTAMP_TYPE=TIMESTAMPTZ
JSON_STRING_TYPE=TEXT
JSON_NSTRING_TYPE=TEXT
SSH=
DRIVER_VERSION=42.7.3
DRIVER=postgresql-${DRIVER_VERSION}.jar
if [ ! -f test/${DRIVER} ]; then
curl -o test/${DRIVER} ${MAVEN2}/org/postgresql/postgresql/${DRIVER_VERSION}/${DRIVER}
fi
cat << EOD > ${OUT}
  ssl: true
EOD
cat << EOD > ${IN}
  query: SELECT * FROM "${SCHEMA}"."${TABLE}"
  ssl: true
EOD
