HOST=
PORT=1433
USER=
PASSWORD=
DATABASE=
SCHEMA=
TABLE=
TIMESTAMP_TYPE=DATETIME
JSON_STRING_TYPE='VARCHAR(max) COLLATE Latin1_General_100_CI_AS_SC_UTF8'
JSON_NSTRING_TYPE='NVARCHAR(max)'
SSH=
DRIVER_VERSION=12.6.1.jre8
DRIVER=mssql-jdbc-${DRIVER_VERSION}.jar
if [ ! -f test/${DRIVER} ]; then
curl -o test/${DRIVER} ${MAVEN2}/com/microsoft/sqlserver/mssql-jdbc/${DRIVER_VERSION}/${DRIVER}
fi
cat << EOD > ${OUT}
EOD
cat << EOD > ${IN}
  query: SELECT * FROM [${SCHEMA}].[${TABLE}]
EOD
